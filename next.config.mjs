/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options can go here.
  // For now, even an empty config can help stabilize behavior.
};

export default nextConfig;
